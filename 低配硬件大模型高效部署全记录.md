# 低配硬件（RTX 4060 8G + 16G）大模型高效部署全记录

> 一份以「可行性优先，极致榨干低配硬件」为主线的完整工程复盘。
> 记录了从 AirLLM 逐层流式到 llama.cpp 整模型驻留、再到 FreeToken 跑 MoE 的完整探索历程，
> 包含三大推理引擎（AirLLM / DFlash2 / FreeToken）的深度剖析、
> FreeToken 桌面端的源码级分析，以及总结出的优化心得与后续计划。
>
> 硬件基线：RTX 4060 Laptop（8GB VRAM）+ 16GB RAM + CPUTRT（软件见各章节）

---

## 目录

**第一部分 · 项目背景与目标**
- 1.1 项目缘起
- 1.2 硬件与软件环境清单
- 1.3 铁三角约束：显存 / 内存 / 磁盘

**第二部分 · 整体架构与目录结构**
- 2.1 Plan-A（llama.cpp + GGUF）路线
- 2.2 Plan-B（FreeToken + MoE）路线
- 2.3 目录树说明

**第三部分 · 探索历程（时间线）**
- 3.1 第一波：AirLLM + Qwen3.8-27B 逐层流式
- 3.2 AirLLM 优化方案：NF4 缓存（方案4）
- 3.3 关键拐点：换引擎到 llama.cpp + GGUF
- 3.4 第二波：FreeToken 跑 MoE（gpt-oss-20b）
- 3.5 锁页内存权限解密（WinError 1450 根因）
- 3.6 大型 MoE 的物理内存瓶颈

**第四部分 · 三大引擎深度剖析（AirLLM / DFlash2 / FreeToken）**
- 4.1 AirLLM 剖析
- 4.2 DFlash2 剖析
- 4.3 FreeToken 引擎架构剖析
- 4.4 三方对比总表
- 4.5 可以学习借用的地方
- 4.6 FreeToken 桌面端功能架构深度剖析（源码级）
- 4.7 如何复用到其他推理引擎（llama.cpp 等）
- 4.8 FreeToken 启动流程 / 模型加载机制 / 多请求并发策略
  - 4.8.1 启动流程 / 4.8.2 模型加载机制 / 4.8.3 多请求并发策略

**第五部分 · 关键决策与经验教训**
- 5.1 锁页内存 / 管理员权限 / pagefile 三角
- 5.2 GGUF 不可逆与格式缺口的教训
- 5.3 各模型实测性能数据汇总表

**第六部分 · 核心心得：对低配硬件跑大模型的启发**
- 6.1 优先"换引擎"而非"调参"
- 6.2 MoE 架构是低配硬件出路，但受物理内存硬约束
- 6.3 量化选型甜点规律
- 6.4 16G 内存是比 8G 显存更硬的瓶颈

**第七部分 · 后续优化与完善计划**
- 7.1 短期：基于 gpt-oss-20b 的应用落地
- 7.2 中期：更多小型 MoE / 其他引擎
- 7.3 长期：多方案并存的工具箱定位

---

# 第一部分 · 项目背景与目标

## 1.1 项目缘起

本项目源于一个朴素而现实的诉求：**在一台 RTX 4060 Laptop（8GB 显存）+ 16GB 内存的普通笔记本上，把「差不多的开源大模型」跑起来，且尽可能快。**

这听起来像悖论——现代大模型动辄几十上百 GB，单靠 8G 显存显然装不下。但项目从一开始就确立了一个核心原则：

> **可行性优先于速度。** 愿意为了「跑起来」接受极慢推理，也要让大模型在低配硬件上真正可用。

这条原则贯穿了整个探索过程，也决定了方案取舍：先用 AirLLM 证明「能跑」，再逐步追求「跑得快」。

## 1.2 硬件与软件环境清单

| 类别 | 项目 | 配置 |
|---|---|---|
| **GPU** | RTX 4060 Laptop | 8GB VRAM，AdaLovelace 架构，PCIe 13GB/s |
| **CPU** | Alderlake 移动端 | 6 核 |
| **内存** | DDR | 16GB RAM（后调 pagefile 至 64GB） |
| **磁盘** | NVMe | ~748GB 可用（D 盘） |
| **系统** | Windows | NVIDIA 驱动 610.88（支持 cu130 后端） |
| **Python** | 3.11.4 / uv 0.12.5 | 虚拟环境 exl2-venv / freetoken-venv |
| **引擎** | PyTorch 2.11+cu128 / ExLlamaV3 / llama.cpp b10472 / FreeToken | 多引擎并存 |

## 1.3 铁三角约束

低配硬件跑大模型，本质是三项资源的三角博弈，任何一项都是硬上限：

```
显存 8GB ──── 决定 GPU 能驻留多少层/多少专家
  │
内存 16GB ─── 决定整模型能否常驻、锁页能锁多少（MoE 专家硬瓶颈）
  │
磁盘 ~748GB ─ 决定能下载/缓存多少模型（相对最宽裕，但 GGUF 不可逆）
```

**贯穿全文的核心洞察：16GB 物内物理内存才是最硬的瓶颈，而非 8GB 显存。**

---

# 第二部分 · 整体架构与目录结构

项目最终收敛为两条并行路线，各自独立自包含、可整体迁移。

## 2.1 Plan-A（llama.cpp + GGUF）路线

**定位**：把量化后的整模型常驻内存，用 llama.cpp 做 CPU/GPU 混合推理。

- 引擎：llama.cpp（b10472，CUDA 12.4 版）`D:\A_BDBS\LLM-LowSpec\Plan-A\qwen38-gguf-llamacpp\llama\bin\`
- 模型：`Qwen3.8-27B-UD-Q3_K_XL.gguf`（12.2GB）
- 关键参数：`-ngl 33`（GPU offload 甜点）
- 实测：**~2.4 tok/s**（vs AirLLM 0.025 tok/s，约 100x 提速）

详细操作见 `D:\A_BDBS\LLM-LowSpec\Plan-A\qwen38-gguf-llamacpp\README.md`。

## 2.2 Plan-B（FreeToken + MoE）路线

**定位**：用 FreeToken 跑 MoE（混合专家）模型，借助 GPU+CPU 协同与专家 offload。

- 引擎：FreeToken Desktop + `ft serve`
- 模型：
  - `models\gpt-oss-20b`（safetensors，12.8GB）— **已跑通**
  - `models\Qwen3.6-35B-A3B-NVFP4`（21.8GB）— 因锁页内存限制未能运行
  - `models\gemma-4-12b`（GGUF 6.26GB）— 架构不适配，弃用
- 关键：需要「锁定内存页」权限 + 管理员身份运行

## 2.3 目录树说明

```
D:\A_BDBS\
├── LLM-LowSpec\                     # 低配硬件大模型部署总目录
│   ├── Plan-A\                      ← llama.cpp + GGUF 路线
│   │   ├── qwen38-gguf-llamacpp\    # 独立自包含：引擎+模型+说明
│   │   │   ├── models\Qwen3.8-27B-UD-Q3_K_XL.gguf
│   │   │   ├── llama\bin\           # llama-cli / llama-server / llama-bench
│   │   │   ├── README.md
│   │   │   ├── Qwen3.6-3.8版本清单.md
│   │   │   └── Qwen模型科普.md
│   │   ├── models\Qwen3.8-27B-FP8\  # 原版权重（非 GGUF）
│   │   └── qwen38 相关脚本
│   └── Plan-B\                      ← FreeToken + MoE 路线
│       ├── app\FreeToken Desktop\   # FreeToken 桌面端
│       ├── models\
│       │   ├── gpt-oss-20b\         # 已跑通
│       │   ├── Qwen3.6-35B-A3B-NVFP4\  # 旗舰，受物理内存限制
│       │   └── gemma-4-12b\         # 弃用
│       └── scripts\锁定内存页权限工具\  # 一键授予/还原锁页权限
├── llama.cpp\                       # 裸引擎（A_BDBS 根，非项目内）
├── exllamav2-models\                # 早期 EXL2 模型/GGUF 中转
└── exl2-venv\                       # ExLlamaV3 虚拟环境
```

---

# 第三部分 · 探索历程（时间线）

## 3.1 第一波：AirLLM + Qwen3.8-27B 逐层流式

**起点认知**：8G 显存塞不下 27B model，于是选择 AirLLM 的「逐层流式」方案——每一层计算时从磁盘加载到显存，算完即释放。

- 模型：Qwen3.8-27B dense，FP8 原始权重约 28GB
- 引擎：AirLLM
- **实测：~0.025 tok/s（约 40 秒/字）**，慢到几乎不可用

**根因**：每生成一个 token 就要把 36 层权重从磁盘逐层过一遍，CPU 端 FP8 反量化 + 全层磁盘 I/O + dense 27B 计算，三者叠加构成不可逾越的瓶颈。

## 3.2 AirLLM 优化方案：NF4 缓存（方案4）

在 AirLLM 框架下做了一系列尝试，最终只有一个方案有实质提升：

| 方案 | 做法 | 结果 |
|---|---|---|
| 方案1 增加常驻层 | `num_resident_layers` | ❌ 无提升但显存翻倍（2.1→5.4GB） |
| 方案4 **NF4 缓存** | 用 bitsandbytes 把解量化缓存压成 NF4 4-bit，加载时再反解到 GPU | ✅ **1.97x**，14.7GB→8.3GB |
| 方案5 减 KV | 缩短 max_seq_len | ❌ 无明显提升 |
| 方案6 架构极限分析 | Qwen3.8 无小 MoE 可用模型 | 转向方案7 |

**结论**：方案4（NF4 缓存）把 620s 压到 315s（8 token），是 AirLLM 唯一亮眼优化。但这只是「从极慢到慢」，仍远不够用。

## 3.3 关键拐点：换引擎到 llama.cpp + GGUF（方案7）

**决定性转变**：不再在 AirLLM 里调参，而是**换推理引擎**。

- 用 Unsloth 量化 `Qwen3.8-27B-UD-Q3_K_XL.gguf`（12.2GB）
- 12.2GB 模型可**整模型常驻 16GB RAM**，无需逐层流式
- llama.cpp `-ngl 33` 做 CPU/GPU 混合
- **实测：~2.4-2.5 tok/s ≈ AirLLM 的 100x**

**核心启示**：「换引擎」带来的数量级提升，远超任何「调参」优化。

## 3.4 第二波：FreeToken 跑 MoE（gpt-oss-20b）

Plan-A 解决 27B dense 后，想上更现代的 MoE 架构（如 Qwen3.6-35B-A3B），于是引入 FreeToken。

- 用管理员身份 + 授予「锁定内存页」权限后，FreeToken 能 `cudaHostRegister` 大块固定内存
- **gpt-oss-20b（12.8GB）成功加载运行** ✅
- FreeToken 自动选了 `hybrid` 后端，检测到锁页预算不够时把部分 MoE 层锁定给 CPU 解码

## 3.5 锁页内存权限解密（WinError 1450 根因）

**症状**：`cudaHostRegister failed: out of memory`（WinError 1450），连 0.2GB 都失败，但物理内存充足。

**根因链**：
1. FreeToken 加载 MoE 专家需要 `cudaHostRegister`（固定内存，pinned memory）
2. Windows 的固定内存分配受 `SeLockMemoryPrivilege`（锁定内存页）权限配额控制
3. 普通用户进程默认**没有该权限** → CUDA pinned 池很小 → 分配失败

**解决**：编写一键脚本授予该权限（自动备份原始策略、可一键还原）+ 重启 + 以管理员身份运行 FreeToken。

## 3.6 大型 MoE 的物理内存瓶颈

排查中发现 FreeToken 的 MoE 内存管理有一个**硬性设计约束**：专家权重通过 `cudaHostRegister` **OS-lock 到物理内存**（不可换页）。

这意味着：**专家权重必须 ≤ 物理内存**（16GB）。即使授予了锁页权限：
- gpt-oss-20b 专家 ~10.5GB ✅ 可跑
- **Qwen3.6-35B NVFP4 专家 ~16.9GB ❌ 超 16GB 物理内存，无法整体锁进内存**

所以 Qwen3.6-35B-A3B-NVFP4 旗舰最终未能运行——这不是权限问题，而是**物理内存的绝对对上限**。

---

# 第四部分 · 三大引擎深度剖析（AirLLM / DFlash2 / FreeToken）

## 4.1 AirLLM 剖析

### 设计理念
**「分层推理（layer-wise inference）」**：模型过大无法驻留内存时，每次前向只把一层权重从磁盘加载到 GPU，算完即释放，循环处理所有层。

### 优点
- **门槛极低**：只要磁盘够装，任何模型理论上都能跑（如 40GB+ BF16）。
- **内存友好**：峰值显存只需容纳单层权重 + 激活。
- **多模态支持**：可处理视觉输入。

### 缺点
- **极慢**：每 token 遍历全部层=反复磁盘 I/O + CPU 反量化，实测 27B dense 仅 0.025 tok/s。
- **dense 模型尤其吃亏**：MoE 因每 token 只激活部分专家，反而相对快些。

### 实际数据（本机）
| 模型 | 架构 | 速度 |
|---|---|---|
| Qwen3.8-27B dense | FP8 逐层 | ~0.025 tok/s |
| Qwen3.6-35B-A3B | FP8 动态加载 | ~0.4-0.45 tok/s |

### 适用场景
**仅在模型量化后仍无法放进 RAM 时兜底**（如 40GB+ BF16）。一旦模型量化到能进 RAM（如 12GB GGUF），llama.cpp 提供 ~100x 提速，AirLLM 就该退役。**本项目将其定性为「超大模型应急备用」。**

## 4.2 DFlash2 剖析

### 设计理念
**「投机解码（Speculative Decoding）」**：用一个小且快的草稿模型（draft model）快速生成候选 token，再用大目标模型验证，一次性接受多个正确 token，从而加速。

### 优点
- 理论上可显著提速（高草稿命中率时）。
- 与目标模型架构无关的通用加速层。

### 缺点（本机实测暴露）
- **草稿模型缺失是硬伤**：对 DeepSeek V4 / GLM 系列无目标专用的草稿模型（DFlash2 需要 target-specific draft）。
- **FP8 动态加载下无加速**：测得草稿 token 接受率仅 **5.3%**，几乎不产生收益。
- 需目标模型共享 lm_head 的草稿结构，当前 config 不满足。

### 实际数据
| 配置 | 结果 |
|---|---|
| DFlash2 + FP8 动态加载 | ~0.95x-1.16x，无实质加速 |
| 草稿接受率 | 5.3%（隐藏状态对齐失败 + 分布不匹配）|

### 结论
DFlash2 在 FP8 动态加载架构上**无效**，已从部署移除。其理念（投机解码）是方向正确的好思路，但**触发成本要求目标+草稿高度对齐**，不满足就是负收益。

## 4.3 FreeToken 架构剖析（基于源码）

FreeToken 是一个**为消费级硬件设计的、edge-native 的 MoE serving 引擎**。以下分析基于本机安装的源码（`%LOCALAPPDATA%\FreeToken\venv\Lib\site-packages\freetoken\`）。

### 4.3.1 分层架构（四大组件）

从 `cli.py` 的 `COMMANDS` 表可见命令体系；从 `daemon/server.py` 可见守护进程设计；从 `launch.py` 可见多进程调度。整体是**「守护进程 + API 服务 + 引擎核心」三层**：

```
/--------------------/    /--------------------/    /--------------------/
|  桌面端 / CLI / 终端UI|→ |  ft daemon 守护进程 | → |  ft serve 引擎核心  |
|  (Tauri / ft shell)  |   |  (torch-free, 常驻) |   |  (多进程 scheduler)|
|                     |   |  控制面 port 1900   |   |  推理面 port 1919  |
\--------------------/    \--------------------/    \--------------------/
```

### 4.3.2 守护进程（daemon/server.py）—— 工程美学亮点

`daemon/server.py` 的注释直接点明设计目标：**"the persistent, torch-free engine supervisor"**（常驻、**不 import torch** 的引擎监管）。

精妙设计包括：

1. **单实例锁**：`SingleInstance(state_dir/daemon.pid)`，两个 daemon 不能共有一引擎——"The ONE hard refusal"。
2. **信号卫生**：`_install_signal_hygiene` 忽略 SIGHUP/SIGPIPE，防止 WSL 中继关闭或客户端断连误杀 daemon。
3. **OOM 收割者**：`_start_oom_reaper` 周期性重写 serve 进程树的 `oom_score_adj`，让 mp-spawn 出来的 worker 始终是「优先被 OOM 牺牲的对象」。
4. **引擎比守护进程长寿**：默认 `manager.detach()`（引擎存活到 daemon 退出后），下次启动时 **re-adopt** 仍在运行的 serve。
5. **降级启动**：#163 注释 "Degraded start: re-adoption must never keep the daemon from booting"。
6. **优雅停机**：显式 `uvicorn.Server` + `timeout_graceful_shutdown=3`，处理桌面端持握无限 SSE 连接导致无法退出的坑。

> **最大亮点**：daemon 端**整个模块不 import torch**（连 `Flame bool` 都不用），只是进程管理和 HTTP 控制面，彻底隔离了「重引擎」与「轻监管」，daemon 可以毫秒级启动、崩溃也不拖垮引擎。

### 4.3.3 服务端多进程模型（launch.py）

`launch_server` 用 `mp.spawn` 起多个 worker：

| 进程 | 职责 |
|---|---|
| `freetoken-TP{i}-scheduler` | 真正的推理引擎（每 TP rank 一个） |
| `freetoken-tokenizer-{i}` | Tokenizer 前置处理（ZMQ 通信） |
| `freetoken-detokenizer-0` | 流式反 decode |

通过 ack_queue 的协议协调就绪状态：`("progress", desc, done, total)` 上报加载进度、`("meta", ...)` 上报缓存开销、`"Scheduler is ready"` 表示就绪。**出错时先 push `("error", reason)` 再退出**，让监督者/桌面端弹窗能展示真实原因而非笼统的"加载失败"。

Windows 下无 `os.setpgrp` 的进程组管理，做了 `_detach_process_group` 兜底，避免 ^C 误杀引擎（#31-47 注释讲得很清楚）。

### 4.3.4 推理核心（core.py）

`core.py` 定义调度数据结构：

- `SamplingParams`：温度/top_k/top_p/stop_strs，`is_greedy` 属性判断贪婪解码。
- `Req`（请求）：核心的请求生命周期对象，从 `cpu tensor` 输入，含 `cached_len/device_len/max_device_len` 分页进度。
- `Batch`：prefill/decode 两阶段批次。
- `Context`：全局上下文，持有 page_table、attention/moe 后端、kvcache、linear_state_pool。

### 4.3.5 MoE 引擎（moe/ 目录）

`moe/` 是 FreeToken 的**灵魂**，支持多种专家执行后端：

```
moe/
├── fused.py / fused_mxfp4.py / fused_nvfp4.py / fused_fp8_block.py / fused_q4_0.py
│        ↑ 全部专家常驻 GPU 显存的融合内核（适合 4090 等大显存）
├── offload.py / offload_cache.py / offload_kernels.py
│        ↑ 专家放 CPU/磁盘，按需加载（CPU offload 后端）
├── hybrid  ← 本机 gpt-oss-20b 用到的 hybrid 混合模式
├── benchbw / bench_profile     # 跑分测 CPU/PCIe 带宽，自动选后端！
└── cpu_offload.py / cpu_executor / expert_banks
```

**关键机制**：`ft bench bw` 会实际测量 CPU vs PCIe 带宽，然后 `--moe-backend auto` 据此自动选 hybrid/offload/fused。gpt-oss-20b 日志里能看到：
```
benchbw profile recommends hybrid for 'mxfp4' experts on this GPU
Auto-selected MoE backend: hybrid
```

### 4.3.6 终端 UI（shell/tui.py）—— 也是源码分析的宝藏

`tui.py` 用 `prompt_toolkit` 实现了一个**非常精致的终端聊天**：

1. **实时状态栏**：`ShellStats.format()` 生成 `[decode] Qwen3 | ↓1234 ↑56 12.3 tok/s | cache 4096 lru 96% | kv 128k/256k 50% | ... | vram 4.1GiB`。
2. **动态缓存池**：`/cache` 命令根据模型实际拥有的 pool（moe/kv/mamba/swa）动态生成提示，没有的 pool 不出现。
3. **thinking 齿轮**：`/think` 切换模型推理模式（来自服务端的 `reasoning.gears`）。
4. **按 turn 的信号绑定**：#474 "Bind ^C to cancel this turn" —— per-turn 而非 per-session，规避 prompt_toolkit 抢占 SIGINT 的坑。
5. **流式输出缓冲**：reasoning（推理链）和 content（正文）分开渲染，推理链不进入下一轮 prompt（#674 注释）。
6. **健壮性**："a blip in the status bar must never break the turn" —— 状态栏轮询失败绝不打断生成。

### 4.3.7 支持模型架构（models/ 目录）

源码 `models/` 显示支持的架构非常广：

- **MoE**：`gpt_oss`、`gemma4`、`glm4_moe`、`glm_moe_dsa`、`minimax_m2/m3`、`qwen3_moe`、`qwen3_5_moe`
- **Dense/混合**：`llama`、`mistral`、`qwen2`、`qwen3`、`muse_glimmer`
- **深度优化**：`deepseek_v4`（DSV4 稀疏注意力，自有 `kernel/csrc` 手写 CUDA 内核）、GDN（GatedDeltaNet 线性注意力）混合模型

每个架构目录下都有 `config.py`（配置解析）、`model.py`（模型定义）、`moe.py`（专家逻辑）、`weight.py`。**部分用 C++/CUDA 编译成 `.pyd`**（如 `weight.cp312-win_amd64.pyd`、`nvfp4_banks`、`loader`），核心是手写内核而不是纯 PyTorch，追求极致性能。

### 4.3.8 手写内核（kernel/）

`kernel/csrc/` 含本地调优的 CV CUDA 内核：
- `gguf/`：GGUF 反量化 + MoE kernel（mmq/mmvq/vecdotq 量化矩阵乘）
- `include/freetoken/`：自定义 tensor.h / utils / warp 原语
- `src/`：pynccl（多卡）、radix（前缀缓存）、tensor
- `pinned_tensor.cpp`：**正是锁页内存的底层实现**（`cudaHostRegister` 封装）

### 4.3.9 优点

- **专为 MoE 低配硬件而生**：fused/hybrid/offload 三后端自动检测，10 倍利用率。
- **工程架构教科书级**：daemon 不 import torch、单实例锁、守护引擎、优雅停机的设计极具参考价值。
- **自动调优**：`bench bw` 实测量带宽选后端，无需手工试。
- **内核级优化**：手写 CUDA/Triton，不迷信 PyTorch。
- **交互体验好**：终端 UI 状态栏、动态缓存命令、thinking 控制。

### 4.3.10 缺点

- **MoE 物理内存硬约束**：专家权重必须 `cudaHostRegister` 锁进物理内存，16GB 机器只能跑专家 <16GB 的 MoE。
- **架构适配窄**：GGUF 只原生支持 gemma4（因此 gemma-4-12B dense 反而报错）；其他模型需 HF safetensors。
- **NVFP4 布局依赖 Blackwell**：RTX 4060（Ada）无原生 NVFP4 支持，走反量化慢路径。
- **管理员+锁页权限门槛**：Windows 下需特殊权限，普通用户难上手。

## 4.4 三方对比总表

| 维度 | AirLLM | DFlash2 | FreeToken |
|---|---|---|---|
| **核心思路** | 分层推理（逐层磁盘流式）| 投机解码（草稿模型加速）| MoE 专用，GPU+CPU 协作/offload |
| **对低配硬件的友好度** | 高（内存极小）| 中 | 高（MoE 可跑）|
| **速度** | 极慢（0.025 t/s）| 依赖草稿命中率 | 快（专门针对 MoE）|
| **门槛** | 低 | 需专用草稿模型 | 需锁页权限/管理员 |
| **适用模型** | 任何（内存极限兜底）| 有匹配草稿的模型 | MoE 为主（gemma4/gpt_oss/qwen3/gim/glm）|
| **本机实测** | 27B~0.025 t/s | FP8 下无加速（5.3% 接受率）| gpt-oss-20b 成功 |
| **最大短板** | 每 token 全层 I/O | 草稿难对齐 | 专家必须进物理内存 |

## 4.5 可以学习借用的地方

**从 AirLLM（借鉴理念）**：
- 「分层/流式」思想用于**热加载超大模型**，可解 16GB 内存跑 40GB 模型的燃眉之急（仅作兜底）。

**从 DFlash2（借鉴理念）**：
- 投机解码是方向正确的加速思路，但**必须保证草稿与目标模型分布对齐**，否则负收益。用于思维启发。

**从 FreeToken（可复用大量工程技巧）**：
1. **守护进程与引擎解耦，daemon 不 import torch** —— 秒级启动、独立零担、崩溃隔离。
2. **单实例锁 + 引擎长寿(re-adopt)** —— 服务稳定性和重启快速。
3. **信号卫生 + 优雅停机 + OOM 收割者** —— 生产级稳定性的完整样板。
4. **先实测带宽再选后端（bench bw）** —— 用数据取代人肉调参。
5. **错误先上报原因再退出** —— 崩溃信息最大化：「告诉真实原因，而非笼统失败」。
6. **按 turn 绑定信号、per-turn 清理** —— 交互式 CLI 细节。
7. **状态栏数据全部来自服务端 stats** —— shell 本身零引擎依赖（"No engine imports, no torch"）。

## 4.6 FreeToken 桌面端功能架构深度剖析（源码级）

> 基于本机安装的 FreeToken 源码（`%LOCALAPPDATA%\FreeToken\venv\Lib\site-packages\freetoken\`）及官方 GitHub 仓库（`FlashML-org/FreeToken`）的完整研读。

### 4.6.1 整体架构：三层解耦

```
┌─────────────────────────────────────────────────────────────┐
│  桌面端 (Tauri GUI)                                          │
│  模型库 · 聊天 · 控制台 · 日志 · 设置 · 应用                  │
└──────────────┬──────────────────────────────────────────────┘
               │ HTTP (loopback)
               ▼
┌─────────────────────────────────────────────────────────────┐
│  ft daemon  控制面 :1900  (torch-free, 永不崩溃)              │
│  引擎生命周期 · 日志环 · OOM 收割 · 单实例锁 · 重新收养        │
──────────────┬──────────────────────────────────────────────┘
               │  spawn / signal / tail
               ▼
┌─────────────────────────────────────────────────────────────┐
│  ft serve  推理面 :1919  (多进程 scheduler + tokenizer)       │
│  OpenAI API · Anthropic API · /v1/stats · /health            │
└─────────────────────────────────────────────────────────────┘
```

**核心设计哲学**：daemon **永远不 import torch**，只做进程管理和 HTTP 控制面。CUDA 故障只杀 serve 子进程，daemon 存活并可以重新收养或重启引擎。这是桌面端"稳如磐石"的根基。

### 4.6.2 模型库（Model Library）

**实现方式**：
- 桌面端通过 daemon 的 `/engine/status` 获取当前加载的模型
- 模型下载走独立的下载管理器（不在 daemon 里），下载完成后写入本地目录
- 加载时桌面端调用 `POST /engine/start {model, port, args[]}`
- daemon 收到后 spawn `ft serve --model <path>`，通过 ack_queue 协议监听加载进度
- 加载进度通过 `/health` 端点暴露：`{status: "loading", phase, progress: {done_bytes, total_bytes}}`
- 桌面端轮询 `/health` 渲染进度条

**复用要点**：
- 模型库本质是"本地目录扫描 + 下载管理 + 加载 API 调用"
- 任何推理引擎只要提供"加载模型"的 HTTP API + 进度上报，就能复用这个模式
- llama.cpp 的 `llama-server` 也有 `/health` 端点，但缺少加载进度；需要包装一层

### 4.6.3 控制台（Chat / 对话）

**实现方式**：
- 桌面端直接连 `serve` 的 `:1919` 端口，走 OpenAI 兼容 API（`/v1/chat/completions`）
- 流式输出通过 SSE（Server-Sent Events）实现
- 采样参数（temperature/top_k/top_p）从模型的 `generation_config.json` 读取，通过 `/v1/stats` 的 `model.sampling` 字段暴露给前端
- 桌面端根据模型自动填充默认采样参数

**复用要点**：
- 这是最通用的板块——只要引擎暴露 OpenAI 兼容 API，任何 GUI 都能对接
- llama.cpp 的 `llama-server` 原生支持 OpenAI API，**可以直接对接 FreeToken 桌面端的聊天功能**（改一下 API 地址即可）
- 关键：采样参数的自动发现机制（从模型配置读取推荐值）是体验加分项

### 4.6.4 日志功能（Logs）

**实现方式**（源码级）：
- daemon 内部维护一个 **LogRing**（环形缓冲区，默认 4000 行）
- 每个 serve 子进程的 stdout/stderr 通过 `LogTailer` 实时 tail 并写入 ring
- 桌面端通过 `GET /engine/logs?since=` 以 **SSE（Server-Sent Events）** 流式获取日志
- 支持 `Last-Event-ID` 断点续传（桌面端崩溃重连不丢日志）
- ANSI 转义码被剥离，tqdm 的 `\r` 被折叠（终端友好的日志清洗）

**复用要点**：
- LogRing + SSE 流式日志是通用模式，任何引擎的子进程日志都可以这样管
- 关键设计：日志在 daemon 层收集（不在 serve 层），这样即使 serve 崩溃，日志仍可读
- 环形缓冲区的容量限制防止内存泄漏

### 4.6.5 设置（Settings）

**实现方式**（从引擎设置页反推）：
- 并发上限（`--max-running-requests`）→ 传给 `ft serve` 的启动参数
- CPU 线程数（`--moe-cpu-threads`）→ MoE CPU offload 线程
- 显存预算（`--memory-ratio`）→ VRAM 使用比例
- MoE 计算模式（`--moe-backend auto/hybrid/offload/cpu`）→ 后端选择
- 附加启动参数（文本框）→ 追加到 `ft serve` 命令行

**复用要点**：
- 设置本质是"启动参数的可视化表单"
- 任何引擎的 CLI 参数都可以映射成 GUI 控件
- FreeToken 的"Auto"模式（自动检测带宽选后端）是亮点，llama.cpp 没有类似机制

### 4.6.6 API 给其他 Agent 应用调用

**实现方式**：
- `ft serve` 暴露三套 API：
  - **OpenAI 兼容**：`/v1/chat/completions`、`/v1/completions`、`/v1/models`
  - **Anthropic 兼容**：`/v1/messages`
  - **Responses API**：`/v1/responses`
- 桌面端"应用"板块列出的 Claude Code / Codex / OpenCode 等，本质上都是调用这些 API 的 CLI 工具
- `ft launch <agent> --install-only` 一键安装这些 agent 并配置 API 地址指向本地 1919 端口

**复用要点**：
- **这是 FreeToken 桌面端最可复用的部分**——只要你的引擎暴露 OpenAI 兼容 API，所有 agent 工具（Claude Code、Codex 等）都能直接用
- llama.cpp 的 `llama-server` 已经暴露 OpenAI API，**天然兼容**
- 关键：agent 工具需要知道 API 地址和模型名，FreeToken 桌面端帮用户配置好了

### 4.6.7 应用板块（Applications）

**实现方式**：
- 每个应用是一个预配置的 CLI 工具（Claude Code、Codex、OpenCode、OpenClaw、Hermes、DeepSeek Harness）
- `ft launch <app>` 做三件事：安装 CLI → 配置 API 地址 → 配置模型名
- 桌面端显示"未安装/已安装"状态，点击"安装"触发 `ft launch <app> --install-only`

**复用要点**：
- 这是"模型 → API → Agent 工具"的最后一公里
- 对于 llama.cpp 用户，可以手动配置这些 agent 工具的 API 地址指向 `http://127.0.0.1:8080/v1`（llama-server 默认端口）

### 4.6.8 守护进程（daemon）的工程美学

从 `daemon/server.py` 和 `daemon/app.py` 源码中提炼出的设计亮点：

1. **torch-free 是铁律**：daemon 只 import stdlib + fastapi + uvicorn，连 `Flame bool` 都不用。CUDA 故障只能杀 serve 子进程，daemon 自身"un-crashable"。（[server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py) docstring）
2. **单实例锁**：`SingleInstance(state_dir/daemon.pid)`，两个 daemon 不能共有一引擎——"The ONE hard refusal"。（[server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py#L123-L129)）
3. **信号卫生**：`_install_signal_hygiene` 忽略 SIGHUP/SIGPIPE，防止 WSL 中继关闭或客户端断连误杀 daemon。（[server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py#L66-L76)）
4. **OOM 收割者**：`_start_oom_reaper` 周期性重写 serve 进程树的 `oom_score_adj`，让 mp-spawn 出来的 worker 始终是「优先被 OOM 牺牲的对象」。（[server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py#L87-L101)）
5. **引擎长寿（re-adopt）**：默认 `manager.detach()`（引擎存活到 daemon 退出后），下次启动时 **re-adopt** 仍在运行的 serve。（[server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py#L162-L167)）
6. **降级启动**：re-adoption 失败不阻塞 daemon 启动（"Degraded start: re-adoption must never keep the daemon from booting"）。（[server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py#L162-L167)）
7. **优雅停机**：显式 `uvicorn.Server` + `timeout_graceful_shutdown=3`，处理桌面端持握无限 SSE 连接导致无法退出的坑。（[server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py#L202-L218)）
8. **双线程池隔离**：`lifecycle_pool`（max_workers=1）处理 start/stop/switch，`proxy_pool`（max_workers=8）处理 health/metrics 轮询——防止健康检查风暴饿死操作员的 stop 请求。（[server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py#L173-L174)）
9. **HTTP API 全 camelCase**：`POST /engine/start`、`GET /engine/logs?since=`、`POST /accounting/ack`，桌面端消费统一。（[proxy.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/proxy.py) 的 `ServeProbe` 做 snake→camel 统一契约）
10. **Accounting 持久化**：引擎停止时写入 durable receipt，桌面端崩溃后可通过 `GET /accounting/pending` + `POST /accounting/ack` 重放，不丢计费数据。（[accounting.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/server/accounting.py)/daemon 层）

## 4.7 如何复用到其他推理引擎（llama.cpp 等）

### 4.7.1 方案 A：最小复用（推荐，最快落地）

**只复用 API 兼容层**：
1. 用 `llama-server` 启动模型（已暴露 OpenAI API）
2. 手动配置 Claude Code / Codex 等 agent 工具的 API 地址指向 `http://127.0.0.1:8080/v1`
3. 用任意 OpenAI 兼容 GUI（如 Open WebUI、LM Studio）做聊天界面

**优点**：零开发，立即可用
**缺点**：没有 FreeToken 桌面端的统一管理体验

### 4.7.2 方案 B：中等复用（包装一层 daemon）

**为 llama.cpp 写一个轻量 daemon**：
1. 参考 FreeToken 的 `daemon/server.py` 架构
2. 实现：进程管理 + 日志收集 + `/engine/start` + `/engine/logs` + `/engine/status`
3. llama-server 的 `/health` 端点已有，只需包装加载进度
4. 桌面端用 Tauri/Electron 写，调用 daemon API

**优点**：获得 FreeToken 式的统一管理体验
**缺点**：需要开发 daemon + GUI，工作量中等

### 4.7.3 方案 C：完全复用（fork FreeToken 桌面端）

**修改 FreeToken 桌面端支持多引擎**：
1. FreeToken 桌面端是 Tauri 应用，前端源码未开源（只有二进制）
2. 但 daemon API 是标准 HTTP，可以写一个"llama.cpp adapter"伪装成 FreeToken daemon
3. adapter 负责：把 `/engine/start` 翻译成 `llama-server` 启动命令，把日志转发给 ring

**优点**：复用 FreeToken 桌面端的全部 UI
**缺点**：adapter 开发 + FreeToken 桌面端可能检测引擎版本

### 4.7.4 针对本机硬件的实操建议

已有条件：
- llama.cpp 的 `llama-server`（OpenAI API 就绪）
- gpt-oss-20b 在 FreeToken 上跑通

**最快落地路径**：
1. 用 `llama-server -m <model> -ngl 33 --port 8080` 启动
2. 配置 Claude Code：`set ANTHROPIC_BASE_URL=http://127.0.0.1:8080/v1`
3. 配置 Codex/OpenCode：类似，改 API 地址
4. 聊天 GUI：浏览器打开 `http://127.0.0.1:8080/`（llama-server 内置网页）

**如果要 FreeToken 式的统一管理**：
- 需要开发一个轻量 daemon 包装 llama-server
- 或等 FreeToken 官方支持 llama.cpp 后端（目前不支持）

### 4.7.5 FreeToken 桌面端设计的可借鉴之处

| 设计点 | 说明 | 对应源码位置 | 可复用性 |
|---|---|---|---|
| **三层解耦** | GUI ↔ daemon(torch-free) ↔ serve(推理) | [server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py)、[launch.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/server/launch.py) | ⭐⭐⭐ 任何引擎都可借鉴 |
| **daemon 不 import torch** | 控制面永不因 CUDA 故障崩溃 | [server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py)（docstring 铁律） | ⭐⭐⭐ 生产级稳定性 |
| **引擎长寿(re-adopt)** | daemon 重启后自动收养仍在运行的 serve | [server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py) `manager.detach()` / degraded start | ⭐ 用户体验好 |
| **LogRing + SSE 日志** | 环形缓冲 + 流式日志 + 断点续传 | [logring.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/logring.py)、[tailer.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/tailer.py)、[logfmt.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/logfmt.py) | ⭐⭐⭐ 通用日志方案 |
| **OOM 收割者** | 周期性提高 serve 的 oom_score_adj | [server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py) `_start_oom_reaper` | ⭐⭐ Linux 特有，Windows 用 pagefile |
| **单实例锁** | flock pidfile 防止多 daemon 冲突 | [server.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/daemon/server.py) `SingleInstance` | ⭐⭐ 基础但必要 |
| **采样参数自动发现** | 从模型配置读取推荐值暴露给前端 | [stats.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/server/stats.py) `model.sampling` | ⭐⭐ 体验加分项 |
| **Agent 工具一键安装** | `ft launch <app>` 配置 API 地址 | [control_api.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/server/control_api.py) / api_server | ⭐⭐⭐ 生态整合 |
| **带宽自适应后端** | `bench bw` 实测选 hybrid/offload/fused | [supervisor.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/server/supervisor.py) 启动测量、[cache_budget.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/engine/cache_budget.py) 显存分配 | ⭐ MoE 特有，llama.cpp 不需要 |

## 4.8 FreeToken 启动流程 / 模型加载机制 / 多模型并发策略（源码级）

> 引擎核心（engine、scheduler、model loader）以编译 `.pyd` 分发，但其外围调度子模块仍是 `.py` 源码，以下据此还原机制。

### 4.8.1 启动流程（`ft serve`）

**顶层入口** `launch.py::launch_server()`：

1. **解析参数** → `parse_args()`（`--model`、`--port`、`--moe-backend`、`--memory-ratio`、`--max-running-requests` 等）
2. **GPU 校验**：若有 `--gpu`，先 `resolve_gpu_uuids()` 解析 UUID，**typ o 提前报错**，避免 worker 启动后才炸
3. **spawn 进程树**（`start_subprocess()`）：
   - `mp.set_start_method("spawn", force=True)`（Windows 必需）
   - 每个 TP rank 一个 `freetoken-TP{i}-scheduler` 进程，`target=_run_scheduler`
   - 一个 `freetoken-detokenizer-0` + N 个 `freetoken-tokenizer-{i}`（`--num-tokenizer`）
   - 全部共享一个 `ack_queue: mp.Queue`，用**统一 ack 协议**上报启动状态
4. **`_run_scheduler`**：先 `set_assigned_gpu` 绑定设备 → **此刻才 import torch** → 建 `Scheduler` → `scheduler.sync_all_ranks()` → 有了结果 `set_progress_sink` 把加载进度推给 ack_queue → `torch.inference_mode()` 下 `run_forever()`
   - 任何启动异常：`_report_startup_error(ack_queue, exc)` 先推 `("error", 原因)`，让父进程面上真实原因而非"exited during load"
5. **Supervisor 收编**（`supervisor.py`）：父进程 `drain_ready` 阻塞等 `expected_acks = num_tokenizers + 2` 个 ready ack，期间应用 `("progress", …)` 更新 `LoadProgress`，`("meta", …)` 传吞吐元数据；**任一 worker 加载中死掉 → `WorkerDied` 且带真实原因**；全部就绪后 `on_ready()` 翻转 maintenance gate 为 `serving`
6. **服务运行中**：`run_right_after` 持续监控 liveness——**serve 崩溃后（scheduler crash while answering 200）也触发 `on_failure`**，关掉"半活引擎"窗口；区分正常停机（`is_shutting_down()`）不误报

### 4.8.2 模型加载机制

**加载走"两段式"：先转成 FTW checkpoint，再加载进显存。**

1. **FTW 转换**（首次加载，日志可见 `checkpoint started`）：
   - 源码 `checkpoint/convert.py`：dense 权重 → `Converting dense weights`；MoE → `Converting expert banks`，重排成 expert-major 布局
   - 落盘到 `.convert-<model>`，日志 `wrote FTW checkpoint`，含 `quant_format`（如 `mxfp4_triton` fallback）
2. **bus 自动配置/显存预算**（`engine/cache_budget.py`）：
   - `plan_cache_budget`：`memory_ratio × baseline_free − 权重 − 固定缓存 = 净预算`，**MoE 优先**——先预留 KV reserve，再贪心塞 expert slot，余量归 KV pages
   - 关键决策在 [cache_budget.py](file:///<localappdata>/FreeToken/venv/Lib/site-packages/freetoken/engine/cache_budget.py#L48-L95)：`hi = min(total_experts, max_slots)`，`prefill_overlap` 需要 `>= 2*num_experts` slot，否则关闭 overlap 并降 floor；预算不足时 `assert` 提前报算术错而非 CUDA OOM
3. **模型类注册**（`models/register.py`）：`_MODEL_REGISTRY` 把 `config.json` 的 `architectures` 名（如 `GptOssForCausalLM`、`Qwen3_5MoeForConditionalGeneration`、`Gemma4ForConditionalGeneration`）映射到具体实现模块 + `parse_config`/`iter_weights` 入口。**gemma4 的 GGUF 版走独立的 `parse_gguf_config`/`iter_gguf_weights`**——这就是你之前 gemma GGUF 缺 `gemma4.expert_count` 报错的原因：GGUF 解析器按 MoE 假设取 expert 元数据。
4. **MoE 专家银行**：expert 权重被组织成 `expert_banks`（每层 `[num_experts, …]` 张量），根据 `--moe-backend`（auto/hybrid/offload/cpu）决定驻留（GPU/主机/分片）。

### 4.8.3 多请求并发策略（同一模型下）

> 关键澄清：FreeToken 是**单 serve 单模型**（一个 `ft serve` 加载一个 checkpoint），**没有"多模型同时常驻"**。所谓"并发"是**同一模型下的多请求并发**（`--max-running-requests`），靠调度器 + 显存预算实现。

**请求级并发机制**：

1. **连续批处理（continuous batching）**：`scheduler/prefill.py` 的 `PrefillManager` 维护 `pending_list`，每 pass 用 `PrefillAdder` 贪心准入尽可能多的请求——`try_add_one` 先 `_try_allocate_one` 探测显存，成功才建 `Req` 入 batch。**准入即乐观锁**：`cache_manager.lock(handle)` 后二次校验预算，不足即回滚。
2. **Paged KV + Radix 前缀复用**（`scheduler/cache.py::CacheManager`）：
   - KV 按 **page 分配**（`free_slots` 是页对齐的 token 起始索引），`allocate_paged` 为每请求按需分页
   - `match_req` 用 **radix 树匹配**请求前缀 → 命中直接复用缓存 KV（`cached_len`），**相同系统提示/多轮尾巴零重复计算**
   - 多模态请求**不参与共享前缀缓存**（图片占位符 token 同名不同内容，会串图）
   - `available_size = evictable + free_slots`，用 `_allocate` 触发 LRU 逐出（`prefix_cache.evict`）补页
3. **多资源预算（"多种货币"）**：hybrid(GDN) 模型额外预算 GDN 状态槽（`linear_state_pool` + ping-pong），SWA 模型预算 swa-pool 槽。准入要同时满足 KV + 各池子——三种"货币"都要有额度（`_try_allocate_one` 里 6.1-6.3 的三重校验）。
4. **长提示分块（chunked prefill）**：超过 `--max-extend-tokens`（默认 8192）的 prompt 拆成 `ChunkedReq` 分批 prefill，避免一次加载全部输入导致显存尖峰；SWA 下按 swa 池容量二次截块。
5. **预填充/解码分离（prefill vs decode）**：`PrefillManager`（准入+并行 prefill）与 `DecodeManager`（`inflight_tokens` 影响后续准入预算）职责分离；可通过参数开 **prefill-overlap**（prefill 与 decode 重叠执行，需 `2*num_experts` 的 MoE slot 借双份 buffer）。

**对你 8G/16G 硬件的直觉**：
- 并发上限主要被 **KV 页总量** 而非权重限制——`--memory-ratio 0.85` 时，KV 页取 MoE 塞完后的剩余显存。
- radix 前缀复用是"多轮对话/多请求共享系统提示"提速的关键，也是 FreeToken 在低配机上不至于因重复 KV 而 OOM 的原因之一。
- gpt-oss-20b（你的实战）`max-running-requests=2`，两个请求共享前缀缓存，交换成本被显著压低。

---

# 第五部分 · 关键决策与经验教训

## 5.1 锁页内存 / 管理员权限 / pagefile 三角

| 因素 | 作用 | 关系 |
|---|---|---|
| **pagefile** | 虚拟内存，总量可够（64GB）| ❌ 锁页不走 pagefile |
| **锁页权限** | 允许进程锁定物理内存 | ✅ gpt-oss 必需 |
| **管理员身份** | FreeToken 进程提权 | ✅ 让锁页真正生效 |
| **物理内存** | 锁页的可锁上限 | ⚠️ MoE 专家的最终硬约束 |

**教训**：调整 pagefile 不够，还要锁页权限 + 管理员身份；但三者都满足后，**物理内存大小才是真正的天花板**。

## 5.2 GGUF 不可逆与格式缺口的教训

**教训**：GGUF 的 IQ3_M 权重是**有损 3-bit 量化**，反解回 safetensors 无法还原精度。社区虽有从 Q8_0 逆向恢复的 safetensors 版，但体积飙升到 35-72GB（FP8/FP16），对 16GB 内存机器不现实。

**启示**：**模型格式的选择要一次性想清楚**。它决定了能上哪个引擎：
- GGUF → llama.cpp
- HF safetensors / NVFP4 → FreeToken
- FTW（FreeToken checkpoint）→ FreeToken（最轻，会自动转换）

## 5.3 各模型实测性能数据汇总表

| 模型 | 引擎 | 体积 | 速度 | 结果 |
|---|---|---|---|---|
| Qwen3.8-27B | AirLLM（NF4缓存）| 28GB | 0.025 tok/s | ❌ 放弃 |
| Qwen3.8-27B | llama.cpp Q3_K_XL | 12.2GB | **2.4 tok/s** | ✅ 采用，约 100x |
| Qwen3.8-27B | llama.cpp 全offload | 12.2GB | 0.98 tok/s | ❌ 全offload慢 |
| gpt-oss-20b | FreeToken hybrid | 12.8GB | 可推理 | ✅ 跑通 |
| Qwen3.6-35B-A3B | FreeToken NVFP4 | 21.8GB | 无法加载 | ❌ 物理内存限制 |
| gemma-4-12B | FreeToken GGUF | 6.26GB | 无法加载 | ❌ 架构不适配 |

---

# 第六部分 · 核心心得：对低配硬件跑大模型的启发

## 6.1 优先"换引擎"而非"调参"

**最大启发**：当某个引擎慢到不可用时，先问「有没有更适合的引擎」，而非在慢引擎里死磕。
- AirLLM 调参（NF4 缓存）只带来 2x
- 换成 llama.cpp 带来 **100x**

**判断框架**：模型能否量化到「整模型驻留 RAM」→ 能则用 llama.cpp/FreeToken；否则才考虑逐层流式兜底。

## 6.2 MoE 是低配硬件出路，但受物理内存硬约束

MoE（专家激活）让「大模型跑在 8G 显存」成为可能——但 FreeToken 的锁页机制暴露出**物理内存才是 MoE 的新瓶颈**：

> **MoE 专家维度必须 `≤ 物理内存`**（本例 16GB）。能跑 gpt-oss-20b（10.5GB 专家），跑不了 Qwen3.6-35B（16.9GB 专家）。

## 6.3 量化选型的甜点规律

- **甜点**：Q3_K_XL 级别（如 12.2GB/27B）= 体积精度平衡好，16GB 内存可行。
- **不要全 offload**：8G 显存装不下 12.2GB，全 offload 反而慢（0.98 比 2.4 t/s）。
- **NVFP4** 是 Blackwell 专属，Ada（4060）用不了原生张量核。

## 6.4 16G 内存是比 8G 显存更硬的瓶颈

反复验证的结论：**8G 显存可以通过 offload/专家化解决，但 16GB 物理内存一旦被 MoE 锁页占满，就完全无解**。低配硬件的优化重心应放在「控制常驻内存 ≤ 物理内存」。

---

# 第七部分 · 后续优化与完善计划

## 7.1 短期：基于 gpt-oss-20b 的应用落地
- 在 FreeToken 上实际调 gpt-oss-20b 提问，评估响应质量与速度
- 可用 OpenAI 兼容 API（`ft serve` 暴露的 1919 端口）对接本地工具

## 7.2 中期：更多小型 MoE / 其他引擎
- 锁定 FreeToken 能跑但专家更小（≤12GB）的 MoE，如小型 Gemini MoE、其他 gpt-oss 变体
- 横向对比更多引擎：ExLlamaV3（本机已配好 exl2-venv）、llama.cpp 对 MoE GGUF 的支持

## 7.3 长期：多方案并存的工具箱定位
- 形成「工具箱」：按模型体积/场景选择引擎
  - 12-15GB GGUF → llama.cpp（最快最稳）
  - 专家 ≤12GB MoE → FreeToken（能力更强）
  - 超大模型（>RAM）→ AirLLM兜底
- 可选：把锁页脚本、启动脚本整理成统一管理工具，一键起停任一引擎

---

*文档由探索历程沉淀而来，数据与源码分析均为本机实测/真实源码。*